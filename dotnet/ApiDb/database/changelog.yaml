databaseChangeLog:
  # Include all SQL files for schema creation or updates (always run)
  - includeAll:
      path: sql
      relativeToChangelogFile: true
  # Include all SQL files for seeding data (only run in dev environment)
  - includeAll:
      path: seed
      relativeToChangelogFile: true
      contextFilter: dev
